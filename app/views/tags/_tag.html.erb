<tr>
  <th><%= tag.name %></th>
  <td>
    <ul>
      <% tag.tasks.where(user: current_user).each do |task| %>
        <li>
          <a href="/tasks/<%= task.id %>/edit"><%= task.title %></a>
        </li>
      <% end %>
    </ul>
  </td>
</tr>