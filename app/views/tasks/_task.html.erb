<tr>
  <th><%= task.title %></th>
  <td>
    <ul>
      <% task.tags.each do |tag| %>
        <li>
          <p><%= tag.name %></p>
        </li>
      <% end %>
    </ul>
  </td>
  <td>
    <button type="button" class="btn btn-primary">
      <a href="/tasks/<%= task.id %>/edit">Edit</a>
    </button>
  </td>
</tr>